{
    "name": "nightclub-website",
    "version": "1.0.0",
    "description": "A premium nightclub website",
    "main": "index.html",
    "scripts": {
        "dev": "npx -y live-server --port=8000 --no-browser",
        "start": "npx -y serve . -l 8000",
        "scrape": "node scripts/scrape-instagram.js && node scripts/scrape-stories.js",
        "build": "rm -rf dist && mkdir -p dist/js dist/assests dist/data && cp *.html *.css *.svg *.png robots.txt sitemap.xml audio-logo.js dist/ 2>/dev/null || true && cp assests/*.webp assests/*.mp3 dist/assests/ 2>/dev/null || true && cp -r assests/events dist/assests/ 2>/dev/null || true && cp data/*.json dist/data/ 2>/dev/null || true && cp js/*.js dist/js/ 2>/dev/null || true && node cache-bust.js && echo '✅ Build complete'",
        "preview": "cd dist && python3 -m http.server 8080",
        "instagram:monthly": "node scripts/scrape-instagram.js && node scripts/scrape-stories.js",
        "instagram:schedule": "node scripts/instagram-scheduler.js"
    },
    "keywords": [
        "nightclub",
        "website"
    ],
    "author": "",
    "license": "MIT",
    "dependencies": {
        "axios": "^1.18.0",
        "node-cron": "^3.0.2",
        "qrcode": "^1.5.4",
        "tesseract.js": "^7.0.0"
    }
}

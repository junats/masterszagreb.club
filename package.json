{
    "name": "nightclub-website",
    "version": "1.0.0",
    "description": "A premium nightclub website",
    "main": "index.html",
    "scripts": {
        "dev": "npx -y live-server --port=8000 --no-browser",
        "start": "npx -y serve . -l 8000",
        "build": "rm -rf dist && mkdir -p dist/js dist/assests && cp *.html *.css *.svg *.png robots.txt sitemap.xml audio-logo.js dist/ 2>/dev/null || true && cp assests/*.webp assests/*.mp3 dist/assests/ 2>/dev/null || true && cp js/*.js dist/js/ 2>/dev/null || true && echo '✅ Build complete'",
        "preview": "cd dist && python3 -m http.server 8080"
    },
    "keywords": [
        "nightclub",
        "website"
    ],
    "author": "",
    "license": "MIT"
}
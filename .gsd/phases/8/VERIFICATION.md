## Phase 8 Verification

### Must-Haves
- [x] Style mobile events container (80vh, scrollbar, glass effect) — VERIFIED (evidence: updated `style.css` with `.matrix-container.active` containing `height: 80vh` and `backdrop-filter` in mobile media query)
- [x] Develop Instagram checking bot/script to run once daily for event updates — VERIFIED (evidence: `scripts/instagram-bot.js` created with Apify fetching and CMS posting logic)

### Verdict: PASS

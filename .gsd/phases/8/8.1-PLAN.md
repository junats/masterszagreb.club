---
phase: 8
plan: 1
wave: 1
---

# Plan 8.1: Mobile Events UI and Glass Effect

## Objective
Enhance the mobile events navigation to occupy 80% of the viewport, with a scrollable list and a premium glassmorphism background effect.

## Context
- .gsd/SPEC.md
- .gsd/ROADMAP.md
- index.html
- css/style.css (or relevant CSS file)

## Tasks

<task type="auto">
  <name>Style Mobile Events Container</name>
  <files>index.html, css/style.css</files>
  <action>
    - Add or update the media query for mobile screens (e.g., max-width: 768px).
    - Target the events navigation container.
    - Set `height: 80vh;` and `overflow-y: auto;` so users can scroll.
    - Apply a glass effect using `backdrop-filter: blur(12px);` and `background: rgba(0, 0, 0, 0.5);` (adjust colors to fit the dark theme).
    - Style the scrollbar to be subtle (`::-webkit-scrollbar` width, thumb color).
  </action>
  <verify>grep "backdrop-filter" css/style.css || echo "Check the exact CSS file used"</verify>
  <done>Mobile events container correctly displays glass effect and scrollbar at 80vh.</done>
</task>

## Success Criteria
- [ ] Events list is scrollable on mobile.
- [ ] Container has a frosted glass effect.
- [ ] Container occupies exactly 80% of the vertical viewport.

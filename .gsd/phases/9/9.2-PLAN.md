---
phase: 9
plan: 2
wave: 1
---

# Plan 9.2: "EVENTS" Label UI

## Objective
Add an "EVENTS" label to the left of the hamburger menu.

## Context
- .gsd/SPEC.md
- .gsd/ROADMAP.md
- index.html
- style.css

## Tasks

<task type="auto">
  <name>Add EVENTS Label</name>
  <files>index.html, style.css</files>
  <action>
    - Add a `<span class="events-label">EVENTS</span>` right before or inside the hamburger menu button container (`#morphToggleBtn`).
    - Since `#morphToggleBtn` is likely `position: fixed;` or `absolute;`, we might need to wrap it in a container, or position `.events-label` absolutely relative to the button or the viewport.
    - Style `.events-label` with the site's primary font (`Orbitron` or `Share Tech Mono`), color (white/green), and proper alignment (e.g., `margin-right: 15px`).
    - Ensure it is visible on both desktop and mobile layouts.
  </action>
  <verify>grep "events-label" index.html</verify>
  <done>The text "EVENTS" appears immediately to the left of the hamburger menu icon.</done>
</task>

## Success Criteria
- [ ] "EVENTS" label is visible next to the hamburger menu.
- [ ] Styling matches the premium aesthetic of the site.

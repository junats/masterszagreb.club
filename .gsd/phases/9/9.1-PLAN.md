---
phase: 9
plan: 1
wave: 1
---

# Plan 9.1: Audio Cleanup

## Objective
Remove the background music loop from the website to improve user experience.

## Context
- .gsd/SPEC.md
- .gsd/ROADMAP.md
- index.html
- audio-logo.js
- js/ (any audio-related modules)

## Tasks

<task type="auto">
  <name>Remove Music Loop Logic</name>
  <files>index.html, audio-logo.js, js/*</files>
  <action>
    - Search for `<audio>` tags in HTML.
    - Search for `new Audio()` or `HTMLAudioElement` creation in JS files.
    - Remove the autoplaying or looping music logic while preserving the audio-reactive visual effects (if they can work without a continuous loop or if the reactive visual requires audio, consider how to handle it, but the primary goal is no continuous unwanted background music loop).
    - If there is a background track playing on loop, remove the call to `.play()` or set `.loop = false` and pause it.
  </action>
  <verify>grep -rnw '.' -e 'Audio' || echo "Verify audio files manually"</verify>
  <done>No background music plays on loop when the website loads.</done>
</task>

## Success Criteria
- [ ] Website no longer plays a continuous background music loop.
